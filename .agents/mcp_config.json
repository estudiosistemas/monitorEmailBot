{
  "mcpServers": {
    "render": {
      "command": "npx",
      "args": ["-y", "@niyogi/render-mcp", "start"],
      "env": {
        "RENDER_API_KEY": "${RENDER_API_KEY}"
      }
    },
    "openrouter": {
      "serverUrl": "https://mcp.openrouter.ai/mcp"
    }
  }
}
